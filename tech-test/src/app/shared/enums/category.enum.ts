export enum CategoryEnum {
  house = 'house',
  bureaucracy = 'bureaucracy',
  work = 'work',
}

export enum CategoryEnumFilter {
  all = 'all',
  house = 'house',
  bureaucracy = 'bureaucracy',
  work = 'work'
}
